RewriteEngine On

# Se o arquivo ou diretório físico existir, serve dele normalmente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Se não vai redirecionar tudo para a index.php
RewriteRule ^ index.php [QSA,L]